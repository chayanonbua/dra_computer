# ระบบจัดเก็บข้อมูลครุภัณฑ์คอมพิวเตอร์ (Computer Asset Management)

## ภาพรวมโปรเจกต์ (Project Context)
เว็บภายในสำหรับจัดเก็บและติดตามครุภัณฑ์คอมพิวเตอร์ของหน่วยงาน
ใช้ทดแทนการเก็บข้อมูลด้วยไฟล์ Excel เดิม ซึ่งมีปัญหาเรื่องการอัปเดต
และการดูประวัติย้อนหลัง

- ผู้ใช้งาน: ภายในหน่วยงาน ประมาณ 2-3 คน
- เป้าหมายหลัก: บันทึกครุภัณฑ์ + ติดตามประวัติการซ่อมและการเคลื่อนย้ายได้ครบถ้วน

## Tech Stack
- **Framework:** Next.js (App Router) + React + TypeScript
- **Database:** SQLite (ไฟล์เดียว ดูแลง่าย เหมาะกับผู้ใช้จำนวนน้อย)
- **ORM:** Prisma (จัดการ schema และ query ให้ปลอดภัย อ่านง่าย)
- **UI:** Tailwind CSS
- **Auth:** ระบบล็อกอินอย่างง่าย (ผู้ใช้ 2-3 คน)

## โครงสร้างข้อมูล (Data Model)
1. **Asset (ครุภัณฑ์)**
   - หมายเลขครุภัณฑ์ (unique), ชื่อ/ประเภท, ยี่ห้อ, รุ่น
   - สถานะ: ใช้งาน / ซ่อม / จำหน่าย
   - ผู้ใช้งาน/กลุ่มปัจจุบัน (อ้างอิงตาราง Owner)
   - วันที่ได้มา, หมายเหตุ

2. **Repair (ประวัติการซ่อม)** — ผูกกับ Asset (1 ต่อหลายรายการ)
   - วันที่, อาการ, การแก้ไข, ค่าใช้จ่าย, ผู้ดำเนินการ

3. **Movement (ประวัติการเคลื่อนย้าย)** — ผูกกับ Asset (1 ต่อหลายรายการ)
   - วันที่, ผู้/กลุ่มต้นทาง, ผู้/กลุ่มปลายทาง, หมายเหตุ
   - เมื่อบันทึก Movement ใหม่ ต้องอัปเดตผู้ใช้งานปัจจุบันของ Asset อัตโนมัติ

4. **Owner (ผู้ใช้งาน/กลุ่ม)**
   - ชื่อ, ประเภท (บุคคล/กลุ่ม)

## หน้าจอหลัก (Pages)
- รายการครุภัณฑ์ทั้งหมด — ค้นหาและกรองตามผู้ใช้ สถานะ ยี่ห้อ
- รายละเอียดครุภัณฑ์ 1 ชิ้น — ข้อมูลหลัก + ไทม์ไลน์ประวัติซ่อมและการย้าย
- ฟอร์มเพิ่ม/แก้ไขครุภัณฑ์
- บันทึกการซ่อม / บันทึกการเคลื่อนย้าย
- แดชบอร์ดสรุป — จำนวนครุภัณฑ์, จำนวนที่กำลังซ่อม, สรุปตามกลุ่ม

## แนวทางการเขียนโค้ด (Conventions)
- ใช้ TypeScript แบบ strict, ตั้งชื่อตัวแปร/ฟังก์ชันเป็นภาษาอังกฤษ
- ข้อความที่แสดงบนหน้าจอ (UI labels) เป็นภาษาไทย
- แยก logic การเข้าถึงฐานข้อมูลไว้ในชั้นเดียว (เช่น /lib หรือ server actions) ไม่ปน UI
- ทุกการเปลี่ยนแปลงข้อมูลสำคัญ (ซ่อม/ย้าย) ต้องบันทึกเป็นประวัติ ห้ามเขียนทับข้อมูลเดิม
- เขียน validation ฝั่ง server เสมอ ไม่ไว้ใจข้อมูลจากฟอร์มอย่างเดียว

## การทดสอบ (Testing)
- **เขียน test ทุกครั้งที่เพิ่มฟีเจอร์ใหม่หรือแก้ไขฟีเจอร์เดิม** — ถือเป็นส่วนหนึ่งของงาน ไม่ใช่ของเสริม
- ใช้ Vitest เป็นเครื่องมือทดสอบหลัก
- ให้ความสำคัญกับการทดสอบ business logic ที่พลาดไม่ได้ โดยเฉพาะ:
  - เมื่อบันทึก Movement ใหม่ ต้องอัปเดต Asset.currentOwnerId ให้ตรงกับปลายทางเสมอ
  - หมายเลขครุภัณฑ์ห้ามซ้ำ (unique)
  - การเปลี่ยนสถานะครุภัณฑ์ (ใช้งาน/ซ่อม/จำหน่าย) ทำงานถูกต้อง
- รัน test ทั้งหมดให้ผ่านก่อนถือว่าฟีเจอร์เสร็จ
- เมื่อแก้บั๊ก ให้เขียน test ที่ครอบสถานการณ์ของบั๊กนั้นก่อนแก้ เพื่อกันไม่ให้กลับมาเกิดซ้ำ

## การ Deploy
- ตั้งเป้าขึ้นเซิร์ฟเวอร์ของหน่วยงาน (คาดว่ารองรับ Node.js — **ต้องยืนยันกับทีม IT ก่อน**)
- ถ้าเซิร์ฟเวอร์รัน Node.js ไม่ได้จริง ให้แจ้งเตือนก่อนวางสถาปัตยกรรมที่ผูกกับ Node
- ฐานข้อมูล SQLite เป็นไฟล์เดียว ต้องสำรอง (backup) ไฟล์นี้เป็นระยะ

## หมายเหตุสำหรับ Claude Code
- เริ่มจากตั้งโครงโปรเจกต์และ schema ก่อน แล้วให้ผมรีวิวก่อนสร้างหน้าจอทั้งหมด
- ทำทีละส่วน อธิบายสิ่งที่ทำในแต่ละขั้น เพราะผู้ใช้ต้องการเข้าใจและดูแลต่อเองได้
- ทุกฟีเจอร์ที่ทำเสร็จ ต้องมี test คู่กันเสมอ (ดูหัวข้อการทดสอบ)

## สรุปความคืบหน้า (อัปเดตล่าสุด)

### เสร็จแล้ว
- ตั้งโครงโปรเจกต์ Next.js + Prisma + SQLite + Vitest พร้อมฐานข้อมูลและ seed
- โครงสร้างข้อมูลลำดับชั้น: สำนัก/กอง > กลุ่ม/ฝ่าย > บุคคล (Office > Group > Person) พร้อมหน้าจัดการ (เพิ่ม/แก้ไข/ลบ) และการกันลบที่ทำให้ข้อมูลกำพร้า
- หน้ารายการครุภัณฑ์ทั้งหมด (ค้นหา + กรองตามสถานะ)
- หน้ารายละเอียดครุภัณฑ์ พร้อมไทม์ไลน์ประวัติ
- ฟอร์มเพิ่ม/แก้ไขครุภัณฑ์ (เลือกผู้ถือครองได้ 3 ระดับ, กันหมายเลขซ้ำ)
- บันทึกการซ่อม
- บันทึกการเคลื่อนย้าย (อัปเดตผู้ถือครองปัจจุบันอัตโนมัติใน transaction เดียว)
- ฟีเจอร์จำหน่ายครุภัณฑ์ (บันทึกวันที่+เหตุผล, ติดป้ายในรายการ, ปิดปุ่มซ่อม/ย้ายเมื่อจำหน่ายแล้ว)
- เมนูด้านข้าง (side menu) แสดงทุกหน้า พร้อมหัวข้อแดชบอร์ดแบบ placeholder
- นำเข้าข้อมูลบุคลากรจริงเข้าฐานข้อมูลแล้ว (6 สำนัก/กอง, 34 กลุ่ม, 174 คน)
- ปรับหน้าจัดการโครงสร้างองค์กรให้ใช้งานง่ายขึ้น: accordion พับเก็บได้ (เริ่มต้นพับทุกระดับ), ช่องค้นหา (กรองตามชื่อบุคคล/กลุ่ม/สำนัก พร้อมกางกิ่งที่ตรงอัตโนมัติ), ฟิลเตอร์ตามสำนัก, ปุ่มขยาย/ยุบทั้งหมด — กรองด้วย pure function `filterOrganizationTree` (มี test คู่กัน)

### กำลังทำ
- (ไม่มี)

### เหลือทำ (ก่อน deploy)
- หน้าแดชบอร์ดสรุป (แทนที่ placeholder) — จำนวนครุภัณฑ์ทั้งหมด, กำลังซ่อม, จำหน่ายแล้ว, แยกตามสำนัก/กลุ่ม
- ระบบล็อกอิน (หน้า login ไม่มี side menu, กันหน้านั้นออกจาก layout หลัก)
- Deploy ขึ้นเซิร์ฟเวอร์หน่วยงาน — ต้องยืนยันกับ IT ก่อนว่าเซิร์ฟเวอร์รัน Node.js ได้จริง

### หมายเหตุ
- ทุกฟีเจอร์ที่ทำต้องมี test คู่กัน (Vitest) และรันผ่านก่อนถือว่าเสร็จ
- commit โค้ดเป็นระยะ โดยเฉพาะก่อนแก้ schema หรือรัน migration
- schema ตอนนี้นิ่งแล้ว พยายามเลี่ยงการแก้ฐานข้อมูลโดยไม่จำเป็น
